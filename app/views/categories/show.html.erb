<h1><%= @category.title %></h1>

<ul>
<% @category.items.each do |item| %>
    <h3><li><%= link_to "#{item.title}: #{number_to_currency(item.price.to_f/100)}", item_path(item) %> - <%= button_to 'Add to Cart', line_items_path(item_id: item) %></li></h3>
<% end %>
</ul>